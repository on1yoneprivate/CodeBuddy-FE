import React from 'react';
import InitComponent from '../components/Init';

const Init: React.FC = () => {
    return (
        <div>
            <InitComponent />
        </div>
    );
};

export default Init;